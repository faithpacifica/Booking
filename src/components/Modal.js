import React from 'react';

const Modal = () => {
    return (
        <div className='modal_window'>
            Modal window for directing to Hotels Page
        </div>
    );
};

export default Modal;